export enum RouteNames {
    MAIN = "/",
    GRAMMAR = "/grammar",
    GRAMMAR_LESSON = "/grammar-lesson/:num",
    VOCAB = "/vocab",
    KANJI = "/kanji",
    SETTINGS = "/settings",
    NOT_FOUND = "*",
}
